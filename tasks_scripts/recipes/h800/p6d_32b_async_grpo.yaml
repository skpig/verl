# ===========================================
# reference trial: https://ml.byteintl.net/development/instance/jobs/f026a45dc803fadd?robustRunId=129927&tabState=task_config&trialId=9550565
# ===========================================

actor_rollout_ref:
  model:
    enable_gradient_checkpointing: True
    use_ce_loss_fusion: True
  actor:
    use_dynamic_bsz: True
    ppo_max_token_len: 150000
    fsdp_size: 32
    ulysses_sequence_parallel_size: 8
    fsdp_config:
      param_offload: False
      use_orig_params: True
    profile:
      enable: True
      upload_to_mlx: True
      filename: "actor_xperf_tp4_fsdp_512"
      profile_on_ranks: [0,8,16,24]
  ref:
    use_dynamic_bsz: True
    max_token_len: 50000
    fsdp_size: 32
    ulysses_sequence_parallel_size: 1
    fsdp_config:
      param_offload: True
      use_orig_params: True
  rollout:
    max_token_len: 50000
    tensor_model_parallel_size: 4

critic:
  model:
    enable_gradient_checkpointing: True
    fsdp_config:
      param_offload: False
      use_orig_params: True
  fsdp_size: 32
  ulysses_sequence_parallel_size: 8
  ppo_max_token_len: 400000

trainer:
  offload_train_memory: True