name: unit tests
trigger:
  change
jobs:
  e2e_runnable_tests_2:
    # Merlin - data.aml.verl:v203 
    image: hub.byted.org/reckon/data.reckon.mlx.image_5940:c496dbfa4ce55747428f630c008cba06
    if: ${{ !contains(Event.Change.Title, "WIP") }}
    runs-on:
      cluster: gpu-l20-16
      docker:
        shared-memory: 1024
    envs:
      LIBHDFS_OPTS: -Dhadoop.root.logger=warn,console
    timeout: 30  # timeout 10min
    retry: 2
    steps:
      - name: check env
        commands:
        - nvidia-smi
      - &step_setup_bvc
        name: setup bvc
        commands:
          - curl -s bvc-cdn.byted.org/non_ttp_tao_deploy.sh | sudo bash
          - sudo ln -sf /usr/local/tao/agent/modules/bvc/bin/bvc /usr/bin/bvc
          - mkdir -p /opt/tiger/bvc/bin/; ln -sf /usr/local/tao/agent/modules/bvc/bin/bvc /opt/tiger/bvc/bin/bvc
      - &step_hadoop
        name: setup hadoop
        commands:
          - bash ./tests/tools/setup_hdfs.sh
      - &step_doas
        name: setup doas
        id: doas
        uses: actions/doas
      - &step_dependency
        name: setup dependency
        commands:
          - cd /opt/tiger
          - git clone -b 20250103 git@code.byted.org:seed/seed_models.git
          - cd seed_models
          - pip3 install -e . --user
          - cd /opt/tiger
          - git clone git@code.byted.org:seed/mono_rl.git
          - pip3 install --upgrade byted-wandb
          - pip3 install bytedance.ndtimeline --upgrade
          - pip3 install byted-omnistore==0.7.14
          - pip3 install pylatexenc
          - pip3 install openai
          - pip3 install pydantic==1.10.15
      - name: math v1 ppo async and grpo
        commands:
          - cd -
          - export NUM_STEPS=4
          - export N_GPUS_PER_NODE=8
          - export NCCL_SHM_DISABLE=1
          - export ALPHASEED_USE_NDTIMELINE=1
          - export PYTHONPATH=/opt/tiger/mono_rl:$PYTHONPATH
          - wandb offline
          - ray stop --force
          - ray start --num-cpus=100 --head
          - bash tasks_scripts/ci/run_p6d_0b5_math_v1_ppo_async.sh
          - ray stop --force
