name: unit tests
trigger:
  change
jobs:
  e2e_runnable_tests:
    # Merlin - data.aml.verl:v205 
    image: hub.byted.org/reckon/data.reckon.mlx.image_5940:26def500fbbd577bdfd57f100f6aa993
    if: ${{ !contains(Event.Change.Title, "WIP") }}
    runs-on:
      cluster: NVIDIA_L20_16|hub.byted.org/reckon/data.reckon.mlx.image_5940:26def500fbbd577bdfd57f100f6aa993
    envs:
      LIBHDFS_OPTS: -Dhadoop.root.logger=warn,console
      SEC_TOKEN_PATH: ${{SEC_TOKEN_PATH}}
      CI_DEBUG_ENABLE: 1
    timeout: 30
    retry: 2
    steps:
      - name: check env
        commands:
          - nvidia-smi

      - name: setup dependency
        commands:
          - cd /opt/tiger
          - git clone -b v1.3.0 git@code.byted.org:seed/seed_models.git
          - git clone git@code.byted.org:seed/mono_rl.git
          - pip3 install --upgrade byted-wandb
          - pip3 install bytedance.ndtimeline --upgrade
          - pip3 install byted-omnistore==0.7.14
          - pip3 install pylatexenc
          - pip3 install pydantic==1.10.15

      - name: math v1 ppo async and grpo
        commands:
          - cd /home/code
          - export NUM_STEPS=2
          - export N_GPUS_PER_NODE=8
          - export NCCL_SHM_DISABLE=1
          - export ALPHASEED_USE_NDTIMELINE=1
          - export PYTHONPATH=/opt/tiger/mono_rl:$PYTHONPATH:/opt/tiger/seed_models
          - export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
          - wandb offline
          - ray stop --force
          - ray start --num-cpus=100 --head
          - bash tasks_scripts/ci/run_p6d_0b5_math_v1_grpo_kl0.sh
          - bash tasks_scripts/ci/run_m8_100m_math_v1_grpo.sh
          - bash tasks_scripts/ci/run_p6d_0b5_math_v1_grpo.sh
          - ray stop --force
